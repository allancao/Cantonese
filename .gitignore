node_modules/
.next/
out/
build/
next-env.d.ts
*.tsbuildinfo

# espeak fallback clips are regenerated on every build, so they stay out of git.
# The neural .mp3 clips from scripts/generate-audio-edge.py ARE committed, because
# their endpoint is unreachable from the build sandbox.
public/audio/*.wav

.env*.local
.vercel
.DS_Store
__pycache__/
